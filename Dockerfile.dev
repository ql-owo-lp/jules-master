# Dockerfile for local development
# This file is optimized for hot-reloading.

FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Copy the rest of the app files
COPY . .

# Expose the port the app runs on
EXPOSE 9002

# The development server will be started by the 'command' in docker-compose.yml
CMD ["npm", "run", "dev"]
