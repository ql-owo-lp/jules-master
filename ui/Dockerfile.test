# Use Ubuntu Jammy (22.04) based image for better stability with native modules
FROM mcr.microsoft.com/playwright:v1.48.1-jammy

# Install Go, Python, and sqlite3
RUN apt-get update && apt-get install -y wget build-essential python3 sqlite3 && \
    wget https://go.dev/dl/go1.24.0.linux-amd64.tar.gz && \
    tar -C /usr/local -xzf go1.24.0.linux-amd64.tar.gz && \
    rm go1.24.0.linux-amd64.tar.gz
ENV PATH="/usr/local/go/bin:${PATH}"

WORKDIR /app

ENV CI=true
ENV NODE_OPTIONS="--max-old-space-size=4096"

# Copy source code first
COPY ui/ .
COPY proto/ /proto/
COPY server/ /server/

# Remove lockfiles to force fresh install
RUN rm pnpm-lock.yaml package-lock.json || true

# Install dependencies using npm
RUN npm install

# Rebuild native modules explicitly from source
RUN npm rebuild better-sqlite3 --build-from-source

# Pre-build backend
RUN cd /server && go mod download && CGO_ENABLED=1 go build -o /app/server_bin cmd/server/main.go

# Run E2E tests
CMD ["npm", "run", "test:e2e"]
